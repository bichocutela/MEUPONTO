# Copy to keystore.properties (ignored by Git) and fill locally.
# The release keystore file itself must NEVER be committed.

storeFile=../keystore/meuponto-release.jks
storePassword=
keyAlias=meuponto
keyPassword=
